<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-inverse-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="javascript:void(0)">TransitValidator</a>
    </div>
    <div class="navbar-collapse collapse navbar-inverse-collapse">
      <ul class="nav navbar-nav">
        <li<%= is_active(:visitors) %>><%= link_to 'Home', root_path %></li>
        <li<%= is_active(:pages) %>><%= link_to 'About', page_path('about') %></li>
        <% if user_signed_in? && current_user.admin? %>
          <li><%= link_to 'Admin', rails_admin_path %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="bootstrap-elements.html" data-target="#" class="dropdown-toggle" data-toggle="dropdown">Bruno Perles <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to 'Edit account', edit_user_registration_path %></li>
              <li class="divider"></li>
              <li><%= link_to 'Sign out', destroy_user_session_path, :method=>'delete' %></li>
            </ul>
          </li>
        <% else %>
          <li<%= is_active(:sessions) %>><%= link_to 'Sign in', new_user_session_path %></li>
          <li<%= is_active(:registrations) %>><%= link_to 'Sign up', new_user_registration_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
